<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ControlTemplate x:Key="Templates.CheckBox" TargetType="CheckBox">

        <Grid
            VerticalAlignment="{TemplateBinding VerticalAlignment}"
            HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
            Height="{TemplateBinding Height}"
            MinHeight="{TemplateBinding MinHeight}"
            MaxHeight="{TemplateBinding MaxHeight}"
            Width="{TemplateBinding Width}"
            MinWidth="{TemplateBinding MinWidth}"
            MaxWidth="{TemplateBinding MaxWidth}"
            Background="{TemplateBinding Background}">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Ellipse Stroke="{StaticResource Brushes.Triple}"
                     StrokeThickness="2"
                     VerticalAlignment="Top"
                     HorizontalAlignment="Left"
                     Width="25" Height="25"/>

            <Path x:Name="elem_checkMark"
                  Stroke="{StaticResource Brushes.Triple}"
                  StrokeThickness="2"
                  VerticalAlignment="Top"
                  HorizontalAlignment="Stretch"
                  Data="M 6 11.5 10 16.5 18 8.5"
                  Width="25" Height="25"/>

            <ContentPresenter Grid.Column="1"
                              Margin="5 0 0 0"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>

        </Grid>

        <ControlTemplate.Triggers>

            <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="elem_checkMark" Property="Visibility"
                        Value="Visible"/>
            </Trigger>

            <Trigger Property="IsChecked" Value="False">
                <Setter TargetName="elem_checkMark" Property="Visibility"
                        Value="Hidden"/>
            </Trigger>

            <Trigger Property="IsChecked" Value="{x:Null}">
                <Setter TargetName="elem_checkMark" Property="Visibility"
                        Value="Hidden"/>
            </Trigger>

        </ControlTemplate.Triggers>
        
    </ControlTemplate>

    <Style x:Key="Styles.CheckBox" TargetType="CheckBox">

        <Setter Property="Template" Value="{StaticResource Templates.CheckBox}"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Background" Value="Transparent"/>

    </Style>
    
</ResourceDictionary>