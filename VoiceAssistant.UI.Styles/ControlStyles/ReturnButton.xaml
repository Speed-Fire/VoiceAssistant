<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:Synergy.WPF.Common.Controls;assembly=Synergy.WPF.Common">


    <Style x:Key="Styles.Button.Return" TargetType="controls:NormalButton"
               BasedOn="{StaticResource Synergy.Styles.NormalButton.Base}">

        <Setter Property="CornerRadius" Value="30"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Background" Value="{StaticResource Brushes.Quaternary}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Top"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>

        <Setter Property="Content">
            <Setter.Value>

                <Image Source="{StaticResource Bitmaps.ArrowLeft64}"
                       MaxHeight="60">
                    <Image.RenderTransform>
                        <TransformGroup>

                            <ScaleTransform ScaleX="1.15" ScaleY="1.15" 
                                        CenterX="0.5" CenterY="0.5"/>

                            <TranslateTransform X="-3.5" Y="-3.5"/>

                        </TransformGroup>
                    </Image.RenderTransform>
                </Image>

            </Setter.Value>
        </Setter>

        <Style.Triggers>

            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Opacity" Value="1"/>
                <Setter Property="Background" Value="{StaticResource Brushes.Quinary}"/>
            </Trigger>

        </Style.Triggers>

    </Style>

</ResourceDictionary>